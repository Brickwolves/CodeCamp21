package org.firstinspires.ftc.teamcode.Labs.Basics;

public class DataTypes {

    public static void main(){

        int     demoInteger = 1;        // Integers are whole numbers only
        double  demoDouble = 1.2;       // Doubles can be whole and have decimals
        long    demoLong = 6000000;     // Longs are... well they can hold a long series of digits

        boolean demoBool = true;        // Booleans are either set to true, or false
        boolean demoBool2 = (5 > 2);    // You can also do this! What's the value of demoBool2?

        String demoString = "Hello";    // Strings are a series of characters. We call them strings because it's like
                                        // the characters, H, e, l, l, o, ! are all 'strung' together to form a word!
        String demoString1 = demoString + " world!";
                                        // You can also add strings together using the plus sign
                                        // demoString1 will now contain "Hello world!"





    }

}
